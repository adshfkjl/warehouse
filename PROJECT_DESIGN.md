# 立体仓库管理系统项目设计书

| 项目 | 内容 |
| --- | --- |
| 文档版本 | 3.30 |
| 当前状态 | 需求与总体架构设计 |
| 更新日期 | 2026-08-26 |
| 系统定位 | 可脱离 ERP 独立运行的完整仓储管理系统（WMS） |

## 1. 文档目的

本文档是新立体仓库管理系统的权威需求与总体设计文档。后续新增、修改或删除需求时，必须同步修改本文档，并在“需求变更记录”中登记。

旧系统源码保存在 `warehouse/` 目录。旧程序当前正在现场使用，其 PLC、寄存器、库位、托盘、装载点和设备时序记录作为新系统第一阶段的现场基线；新系统暂时通过兼容适配器调用原有程序提供的 PLC 能力，不直接改写现场控制逻辑。旧系统功能盘点见 `project-reference.md`。

## 2. 建设目标

建设一个能够完全脱离 ERP 独立运行的立体仓库管理系统，覆盖仓储全流程，并统一管理库存、库位、托盘、作业任务、PLC 设备和操作记录。

系统需要满足：

- 没有 ERP、MES 或其他上游系统时仍能完成全部仓储作业。
- 支持手工录入、Excel 导入和可选外部 API 创建业务单据。
- Excel 入库/出库导入属于第一版范围，必须有版本化模板、整批校验、错误报告和导入幂等；导入只创建 WMS 单据，不直接下发 PLC。
- 维护独立、可信、可追溯的库存账。
- 支持入库、出库、移库、盘点和异常处置的完整闭环。
- PLC、数据库或服务重启后，未完成任务能够恢复或进入人工处置状态。
- 外部系统集成是可选能力，禁用后不得影响系统运行。

## 3. 项目范围

### 3.1 本期范围

- 基础资料
- 入库管理
- 出库管理
- 库存管理
- 移库管理
- 盘点管理
- 作业任务与调度
- 库位分配策略
- PLC/WCS 设备控制
- 异常处理
- 用户、角色、权限和审计
- 管理界面
- 报表与数据导出
- 系统配置、备份、恢复和运行监控
- 可选的外部系统集成接口

### 3.2 暂不考虑

- 采购订单及采购结算
- 销售订单及销售结算
- 生产计划、工序和制造执行
- 财务核算、成本和发票
- 客户关系管理
- 供应商关系管理

供应商、客户、部门等可作为仓储单据的关联资料维护，但不实现 ERP 业务逻辑。

### 3.3 第一版范围、入口优先级和状态词典

第一版范围、排除项、手工/PDA、Excel 和外部 API 的入口优先级，以及入库、出库、移库、盘点、库存和设备任务的状态集合与合法流转，统一见 [`docs/status-dictionary.md`](docs/status-dictionary.md)。WMS 自有单据、库存、托盘、库位和任务是唯一业务真相；外部来源不得绕过 WMS 状态机、权限、资源锁或幂等检查。

## 4. 总体架构

采用“模块化单体 WMS + 现有 PLC 接口兼容层”。旧程序当前正在现场使用，PLC 连接、寄存器协议和入库/出库/移库接口作为已验证的生产资产保留。新系统不重新定义现场 PLC 协议，而是在其外部增加 WMS 业务层和适配器。

```text
浏览器/PDA
    |
    v
WMS Web/API
    |- 基础资料
    |- 入库与出库
    |- 库存与盘点
    |- 任务调度
    |- 权限与审计
    |- 报表
    |- 可选集成适配器
    |
    +---- WMS 数据库
    |
    v
现有 PLC 接口/PLC Service
    |- 现有 PLC 连接与心跳
    |- 现有寄存器协议
    |- 现有入库、出库、移库指令
    |- 设备状态采集
    |- 超时、重试与恢复
    |
    v
PLC / 堆垛机 / 输送设备
```

### 4.1 架构边界

- WMS 决定做什么、使用什么库存、分配哪个库位以及任务优先级。
- 现有 PLC 接口负责把设备任务转换为现场指令，并反馈执行状态。
- 新 WMS 通过 `IWarehouseDeviceGateway` 一类适配器调用现有 PLC 接口，不直接在业务代码中写寄存器。
- 第一版 `Warehouse.DeviceGateway` 作为 WMS Worker 内加载的基础设施类库，通过 HTTP 调用独立运行的旧 PLC API；它不是第三个独立服务，也不拥有业务数据库。未来拆成独立服务必须另立设计和迁移计划。
- 现有 PLC 接口继续负责设备连接、协议和现场时序；新系统第一阶段不改动其生产行为。
- PLC 接口不得直接修改新 WMS 的业务单据和库存账。
- WMS 根据设备结果，通过自己的事务更新任务、库位、托盘和库存。
- 库存变化只能通过明确的仓储业务操作产生。
- 可选外部接口不得直接访问 WMS 数据表或 PLC。

### 4.2 现场基线和过渡调用策略

第一阶段以旧程序现场记录和已验证行为作为基线，基线至少包括：

- PLC 编号、IP、端口、Slave ID 和连接方式
- 寄存器地址、地址偏移、数据类型和状态码
- 仓库、货架、排、层、列、库位编码及库位状态
- 托盘/料箱编码、当前所在库位和装载点状态
- 装载点编号、空闲判断和托盘同步规则
- 入库、出库、移库、复位、急停、完成判断和报警处理时序
- 设备并发限制、超时、重试和人工接管规则

现场基线表和未知项责任清单见 [`docs/field-baseline.md`](docs/field-baseline.md)。其中 `BLOCKED` 项只能在现场负责人确认并留存签字或点检证据后解除；源码默认值不等于现场配置。

过渡阶段规则：

1. 新 WMS 通过 `IWarehouseDeviceGateway` 或等价适配器调用原有 PLC 接口。
2. 新 WMS 不直接写寄存器、不直接引用旧控制器，也不在业务代码中复制设备时序。
3. 旧程序继续负责现场 PLC 连接、寄存器协议和设备动作；新 WMS 负责业务单据、库存、任务状态和审计。
4. 旧程序返回的设备结果必须先转换为新 WMS 的统一设备结果，再参与业务状态流转。
5. 旧程序依赖 ERP 的单据查询、库存更新或 ERP 存储过程时，新 WMS 不得把这些依赖带入自己的业务层；应在适配器边界记录依赖并逐步替换为 WMS 自有数据。
6. 在接口契约测试、PLC 模拟测试和现场回归验证完成前，不得修改旧程序的 PLC 指令时序。

API 运行时组合规则：开发和测试环境必须在 API 启动时注册完整的控制器依赖图，并默认注入 `SimulatedDeviceGateway`；不得因为缺少 ERP、真实 PLC 或生产连接串而使 API 无法启动。设备网关以 `IWarehouseDeviceGateway` 抽象注入，业务服务共享统一任务调度器；依赖当前 HTTP 用户的异常和高风险服务使用请求作用域，禁止由单例捕获请求作用域对象。真实 PLC 适配器必须由单独审核的部署配置显式启用，默认配置不得连接现场设备。

### 4.3 业务数据持久化模式

WMS 的库存余额、不可篡改库存流水和库存操作幂等键必须以 SQL Server 为业务真相。`InventoryService` 允许在没有数据库连接的开发/契约测试中使用显式内存实现，但 API 只有在 `Wms:PersistenceMode=SqlServer` 且提供 `ConnectionStrings__WmsDb` 时才启用 SQL 持久化；生产配置不得回退到内存模式。SQL 实现必须在同一个短数据库事务中完成幂等键校验、余额版本校验、库存流水写入和余额更新，设备执行等待期间不得持有该事务。服务重启后必须从余额和流水恢复，重复幂等键只能返回原结果，摘要冲突必须拒绝。

Task 9.5 将业务任务、任务状态历史、资源锁和任务幂等键提升为 SQL Server 业务真相。任务状态版本、资源锁版本和必要的唯一业务键由 EF Core/SQL Server 约束保护；资源锁按 `ResourceType + ResourceId` 唯一识别，支持租约抢占、续租和释放，只有过期锁才允许被新任务重新取得。创建任务、状态迁移、历史追加、幂等键登记以及锁获取/续租/释放均使用独立短事务，PLC 调用、设备等待、轮询和回调期间不得持有数据库事务。服务重启后必须能够恢复未完成任务、完整状态历史、有效资源锁和幂等记录；内存实现只允许在明确的单元测试、契约测试或显式 InMemory 开发模式使用，生产 SQL 模式不得隐式回退。

Task 9.5 已完成上述 SQL 持久化基础设施并通过 Docker SQL Server 重启恢复验证。Task 9.6 已将调度器、Worker、设备结果和入库/出库/移库/盘点资源锁接入持久化边界：任务调度上下文、工作流引用/快照、设备租约和状态历史可恢复；有效快照标记 `RecoveredFromSnapshot`，缺失或非法快照进入 `BlockedMissingBusinessState`，未知物理结果不得重复下发或释放资源。主代理已完成构建、普通测试、Docker SQL 任务重启/跨进程租约和消息持久化验证，但四类业务服务的订单、分配、盘点范围和库存结算聚合仍主要保存在进程内，完整跨进程业务恢复拆分为 Task 9.7，故 Task 9.6 保持 `DONE_WITH_CONCERNS`，真实 PLC 和现场恢复继续保持 `FIELD_PENDING`。

Task 9.7A/9.7B 将分两步为入库、出库、移库和盘点建立 SQL 业务聚合事实、版本化状态和启动重建流程；在两项任务完成前，不能将任务元数据恢复等同于完整仓储业务闭环恢复。

退出过渡模式的条件：

- WMS 已拥有完整的 PLC/设备配置基线和版本记录。
- 入库、出库、移库、状态读取和异常恢复均有契约测试及现场回归记录。
- 旧接口中的 ERP 查询、单据回写和库存修改已从设备调用链剥离。
- 新设备网关可以在不改变现场行为的情况下替换旧接口调用。
- 已完成明确的切换、回滚和现场停机方案。

## 5. 核心数据归属

| 数据 | 归属 |
| --- | --- |
| 物料、单位、条码、批次规则 | WMS 基础资料 |
| 仓库、库区、巷道、货架、库位 | WMS 基础资料 |
| PLC、设备、装载点、寄存器协议 | 现有 PLC 接口/兼容层 |
| 托盘、料箱和容器 | WMS |
| 入库单、出库单、移库单、盘点单 | WMS |
| 库存、库存占用、库存流水 | WMS |
| 作业任务及状态历史 | WMS |
| PLC 原始状态、报警和指令日志 | PLC/WCS 模块 |
| 用户、角色、权限和审计日志 | WMS |

## 6. 功能设计

### 6.1 基础资料

- 仓库、库区、巷道、货架、排、层、列和库位维护
- 装载点、输送线、堆垛机和 PLC 配置
- 物料编号、名称、规格、单位和条码
- 托盘、料箱及容器类型
- 批次、生产日期和有效期规则
- 库位容量、尺寸、重量限制、禁用和锁定状态
- 入库、出库、移库、盘点和库存调整原因代码
- 供应商、客户、部门等关联资料
- 编号规则和系统参数

第一版基础资料由 WMS 自有数据库维护，不读取 ERP 表。仓库、库区、巷道、货架、库位、装载点、设备、物料、容器和托盘通过数据库迁移建立；库位编码、托盘编码、设备编号和装载点编码必须唯一。库位记录容量、最大重量、长宽高、禁用、锁定和现场映射；托盘记录版本号、当前库位/装载点归属和归属状态，并通过并发版本与数据库唯一约束禁止双占用。开发环境迁移包含不连接现场的样例基础资料种子。

### 6.2 入库管理

支持普通入库、退货入库、生产入库和其他入库。

```text
创建入库单
  -> 到货登记
  -> 扫描物料和托盘
  -> 收货与质检
  -> 形成待上架库存
  -> 分配目标库位
  -> 生成上架任务
  -> PLC 执行
  -> 确认入库
```

需要支持：

- 手工创建、Excel 导入和 API 创建入库单
- 收货数量差异
- 批次、生产日期和有效期录入
- 托盘绑定、换托和重量采集
- 自动推荐或人工指定库位
- 部分收货、部分上架、取消和异常结束
- 入库单、收货记录、上架任务和库存流水关联追踪

Task 5.1 的当前实现已冻结入库单状态为 `Draft`、`Receiving`、`Received`、`PutawayQueued`、`Completed`、`Canceled` 和 `Exception`。手工建单及明细收货支持批次、有效期、托盘绑定、重量和收货幂等键；收货结果只生成 `PendingInbound` 记录，库位归属保持为空，不直接增加库位实存。Task 5.2 增加了基于库位状态、尺寸、重量和容量的自动推荐/人工指定、推荐原因记录、装载点有货校验、待入库库存/托盘/目标库位/装载点资源锁，以及通过统一任务调度器提交入库设备任务；重复幂等请求不会重复下发，设备结果确认前库存仍为 `PendingInbound`。Task 5.3 在设备成功结果确认后以独立短事务幂等增加目标库位库存、完成任务并释放资源；失败、超时和物理未知不增加库存且保留资源锁，只有同一入库单的全部待上架任务确认后才完成入库单。当前入库服务、分配、对账和资源锁为内存契约，尚未接入 SQL Server 持久化。

Task 5.1A 已增加版本为 `1.0` 的入库/出库 `.xlsx` 模板和导入服务。入库列为 `OrderNumber`、`MaterialId`、`Quantity`、`BatchNumber`、`ExpirationDate`、`PalletCode`、`WeightKg`；出库列为 `OrderNumber`、`MaterialId`、`Quantity`、`BatchNumber`、`PalletId`、`LocationId`。导入先完成整批校验，错误报告包含行号、列名、错误码和修复建议，任意错误均不会创建部分单据。来源键与文件 SHA-256 摘要用于幂等，重复文件返回原导入结果，来源键绑定其他文件时拒绝；成功导入只调用入库/出库建单服务，不创建设备任务或调用 PLC。当前导入记录和错误报告为内存实现，生产持久化留待后续数据库任务。

### 6.3 出库管理

支持普通出库、生产领料、退货出库和其他出库。

```text
创建出库单
  -> 校验并分配库存
  -> 锁定库存
  -> 生成下架任务
  -> PLC 执行
  -> 装载点确认
  -> 出库复核
  -> 扣减库存
```

Task 5.4 已定义出库单状态 `Draft`、`Allocated`、`Locked`、`Picking`、`AwaitingReview`、`Completed`、`Canceled` 和 `Exception`。出库分配支持指定批次、托盘和库位，并按确定性顺序选择可用库存；分配数量可小于库存余额，库存、托盘和库位通过幂等资源锁防止并发重复分配。Task 5.5 增加出库设备任务、装载点占用校验和锁定、设备结果处理、托盘号/重量/物理状态复核，以及复核通过后的库存扣减；设备失败、超时、未知结果和复核差异不得扣减库存。

需要支持：

- 手工创建、Excel 导入和 API 创建出库单
- 按物料、托盘、批次或指定库位出库
- FIFO、FEFO、指定批次等库存分配策略
- 多明细、多托盘和部分出库
- 缺货、取消、复核差异和异常结束
- 防止重复出库、超量出库和负库存

### 6.4 库存管理

- 实时物理库存
- 可用、锁定、待入库、待出库、冻结和异常库存
- 按仓库、物料、批次、托盘和库位查询
- 库存冻结、解冻和调整
- 库内移库
- 托盘拆分、合并和换托
- 数量、重量、批次、质量状态管理
- 不可篡改的库存流水
- 每次库存变化记录来源单据、任务、操作人和时间
- 库位账、托盘账和物料账一致性校验

第一版库存服务以 WMS 库存流水为唯一变更入口：增加、减少、锁定、解锁、移库和调整都必须携带幂等键及来源单据、任务号、操作人、原因和时间。余额由流水顺序重算；重复幂等请求不得重复入账，超量扣减、冻结/异常库存和并发锁定必须失败并保持原余额。库存服务提交操作时使用短事务语义，业务控制器不得直接修改余额数量。

### 6.5 作业任务和调度

统一管理入库、出库、移库、盘点和回库任务。

任务状态基线：

```text
Created -> Allocated -> Queued -> Dispatching -> SentToPlc -> Executing -> Succeeded
                                                   |-> Failed
Created/Allocated/Queued --------------------------|-> Canceled
异常且无法自动恢复 -------------------------------|-> ManualIntervention
```

设备任务还必须区分 `CancelRequested`、`StopRequested`、`StopConfirmed`、`StopFailed` 和 `PhysicalStateUnknown`。旧接口不支持任务号去重或按任务号查询时，发送后超时直接进入 `PhysicalStateUnknown`，不得自动重试或释放资源。

需要支持：

- 任务优先级和排队
- 同一设备的安全串行控制
- 库位、托盘、装载点和设备资源锁
- 指令幂等，防止重复下发和重复完成
- 超时、有限重试、取消和人工干预
- 急停、断电、服务重启后的任务恢复
- 完整任务状态历史和操作日志

任务状态由 WMS 状态机统一维护。每次迁移都必须记录前状态、后状态、操作者、原因、错误码、UTC 时间和任务版本；`Succeeded`、`Canceled` 与 `ManualIntervention` 受终态保护。已发送到 PLC 或正在执行的任务不得直接转为 `Canceled`，必须经过取消请求和停止协议；无法对账的超时任务必须进入 `PhysicalStateUnknown`，不得自动重试、释放资源或提交库存。

任务并发协调规则：任务命令、设备提交和设备结果均使用带作用域的唯一幂等键，并保存请求/消息摘要；同一键重复且摘要一致时视为安全重放，摘要冲突必须拒绝。库位、托盘、装载点和设备使用带租约到期时间、锁令牌和乐观版本号的资源锁；只有持有者以当前版本续租或释放，过期锁才允许由新任务重新取得。设备命令先在 WMS 短事务中写入 `OutboxMessage` 并记录发送尝试，再调用 PLC；设备等待、轮询和回调期间不得持有数据库事务。结果先写入 `InboxMessage`，按消息 ID、幂等键和结果版本去重，之后由独立短事务更新任务、库存和资源锁。Outbox/Inbox 的状态提交不能替代设备任务状态机；无任务号去重/查询能力的设备发送超时仍必须进入 `PhysicalStateUnknown`，不得自动重发。Task 9.2A 已将两类消息映射到 SQL Server：唯一业务索引、乐观版本字段和租约状态均由 EF Core 管理；入队、抢占、发布/处理、失败回退各自使用可提交/回滚的短事务，尚未接入调度器或跨进程 Worker。

任务调度器按设备串行执行并按优先级选择待调度任务；发送前进入 `Dispatching`，设备接受后进入 `SentToPlc`，轮询和可选回调统一交给结果处理器。设备任务号查询能力可用时，Worker 重启先查询并恢复状态；能力不足或结果无法对账时进入 `PhysicalStateUnknown`，不得重复下发或释放物理资源。调度 Worker 的轮询循环与业务状态提交分离，不能把 PLC 等待过程包在数据库事务中。发送调用在开始前被取消时回到 `Queued` 并释放调度占用；调用已开始但结果未知时必须进入 `PhysicalStateUnknown` 并保持设备资源占用。结果处理必须校验设备任务号归属，并按结果版本单调去重，旧版本不得覆盖新观察。

### 6.6 库位分配策略

- 过滤禁用、故障、锁定或已占用库位
- 校验尺寸、容量和重量
- 同物料集中存放
- 重货优先低层
- 高周转物料靠近出入口
- 批次和质量状态隔离
- 巷道及设备负载均衡
- 支持人工指定库位
- 保存策略命中原因，确保分配结果可解释

### 6.7 移库管理

- 按托盘、物料或指定库位创建移库任务
- 自动推荐目标库位
- 源库位、目标库位和托盘校验
- 移库过程中锁定相关资源
- PLC 完成后原子更新源库位、目标库位和库存位置
- 支持取消、失败恢复和人工处置

Task 5.6 当前实现支持源库位有货、目标库位为空、物料/托盘归属和数量重量校验，并通过统一 `TaskScheduler` 提交 `SubmitTransferAsync`。移库任务锁定源库存、托盘、源/目标库位和设备；设备成功后以幂等短事务执行库存 `Move` 流水，失败或物理未知不移动库存、不释放未确认锁。跨 PLC/跨巷道能力和真实托盘位置更新仍需现场基线确认与后续持久化实现。

### 6.8 盘点管理

- 全库、库区、物料、批次、托盘范围盘点
- 动态抽盘
- 明盘和盲盘
- 盘点期间库存冻结或限制相关作业
- 账面数量、实盘数量和差异记录
- 差异确认、复盘和调整审批
- 盘点任务中断恢复
- 盘点差异和调整报告

Task 6.1 当前实现支持全库、库区、物料、批次、托盘和位置范围筛选；位置范围兼容旧基线 `A1-A8` 货架闭区间及 `prefix-number` 位置闭区间，并按位置/物料/托盘确定性排序。盘点任务保存账面数量、重量、实盘结果、差异状态和装载点；设备辅助盘点通过统一任务调度器逐条排队并串行占用装载点，不直接覆盖库存。

Task 6.2 增加盘点差异工作项和调整审批：支持明盘/盲盘视图、差异原因、复盘替换和 `FreezeOnDifference` 冻结策略；调整工作项记录初盘/复盘结果、审批人、二次授权、状态历史和应用流水号。只有授权通过后才调用 `InventoryService.AdjustAsync`，调整编号作为库存流水幂等键；重复确认不会重复写流水，库存调整失败时保持 `Approved` 以便重试。移动托盘上架预约按盘点明细幂等创建，并严格区分 `Reserved`、`Sent` 和 `Completed`。当前实现为内存应用契约，库位编码到持久化库位 ID 的映射、数据库持久化和真实设备确认留待后续任务与现场门禁。

### 6.9 PLC 接口兼容和设备控制

旧系统的 PLC 接口目前正在使用，重写系统必须优先保证现场设备稳定：

- 保留现有 PLC 连接方式、寄存器地址、地址偏移、Slave ID 和设备时序。
- 保留现有入库、出库、移库、复位、状态读取和参数设置能力。
- 新 WMS 不直接引用 ERP 数据库，也不要求 PLC 接口继续依赖 ERP。
- 新增设备网关/适配器，将 WMS 任务转换为现有 PLC 接口的请求格式。
- 设备接口返回成功、失败、超时、离线和未知状态，新 WMS 负责业务状态转换。
- 第一版以 `GetStatusAsync` 定时轮询为设备结果来源；旧接口若支持回调，回调只能先写入 Inbox。轮询和回调必须转换为同一设备观察对象，并按设备任务号、结果版本和幂等键去重。
- 旧接口只有在明确支持 WMS 任务号去重且可以按任务号查询时，才允许自动重试；发送后超时且无法按任务号查询时必须进入物理状态未知，未经对账或人工确认不得重试、释放资源或再次下发。
- 旧 PLC 服务作为独立进程或独立项目运行，升级时必须支持回滚。
- 在没有完整自动化测试和现场验证前，不修改现有 PLC 指令时序。
- 通过接口契约测试和 PLC 模拟器验证适配器，真实设备只进行受控测试。

兼容接入分两个阶段：

1. **过渡模式**：新 WMS 通过旧程序提供的 PLC 操作接口调用设备，旧程序保持原样运行。需要确认旧接口所依赖的数据库连接、库位查询和状态回写是否可以由兼容数据库或适配配置满足。
2. **稳定模式**：将旧程序中纯 PLC 连接和设备指令部分独立为设备网关，WMS 只通过网关接口提交设备任务；旧 ERP 查询、单据回写和库存更新逻辑不进入新系统。

“直接使用现有 PLC 接口”表示复用已经在现场验证过的设备调用契约，不表示新 WMS 可以直接引用旧控制器、直接写寄存器，或继续依赖 ERP 表和存储过程。

- PLC 配置、启停和连接测试
- Modbus TCP 连接、心跳和断线重连
- 寄存器地址、数据类型和偏移配置
- 入库、出库、移库、复位和参数设置指令
- PLC 状态、坐标、速度、重量和完成信号采集
- 装载点和设备占用状态
- 报警码解析
- 通讯超时和有限重试
- 原始指令及响应日志
- 现有 PLC 接口的兼容调用
- PLC 模拟器和自动化契约测试，作为第一阶段设备兼容层门禁

### 6.10 异常处理

- PLC 离线或通信超时
- 源库位无货、目标库位有货
- 托盘不存在、重复或条码不一致
- 装载点占用
- 重量、尺寸或容量超限
- 库存与实物不一致
- 任务长时间无响应
- 具备任务号去重/查询能力时的自动重试、能力不足时的物理状态未知、人工重试、取消、恢复和人工确认物理结果
- 所有人工处置必须记录操作人、原因、时间和前后状态
- 异常工作项必须记录来源、类型、严重程度、关联任务、设备任务号、物理状态、托盘、库位、装载点、库存锁和处置历史。
- 重复异常按来源和外部幂等键合并；支持重试、停止请求、重新分配、人工确认物理结果和库存校正，并经过高风险权限校验。
- 第一版异常中心以 `source + externalKey + taskId` 作为活动工作项幂等键；重复告警合并审计，已关闭工作项再次收到同一告警时重开原工作项，不创建并行活动项。
- `PhysicalStateUnknown` 工作项不得直接重试、重新分配、释放资源或标记解决；必须先通过设备对账或“人工确认物理结果并结案”完成物理状态确认。人工确认和库存校正必须经过 `ICurrentUser` 与 `IRiskAuthorizationService` 二次授权，并保存原因、设备观察、处理前后状态和操作人。
- Task 4.5 的第一版实现使用显式内存仓储及动作执行器契约，仍需接入真实数据库、Outbox/Inbox 持久化和跨进程恢复。Task 6.3 已接入开发环境 JWT、刷新令牌轮换、用户/角色/权限、仓库范围、高风险授权和内存审计；接入生产前必须替换为 SQL Server 事务仓储、真实密钥/用户存储和审计持久化，不能将内存测试结果视为生产完成。

### 6.11 用户、权限和审计

- 用户、角色和权限管理
- 登录、刷新令牌和安全退出
- 密码修改、重置和账号禁用
- 按功能和仓库授权
- 库存调整、任务取消、人工完成等高风险权限
- 在完整用户/JWT 模块上线前，业务服务也必须依赖 `ICurrentUser` 和 `IRiskAuthorizationService` 抽象及测试实现，不得跳过高风险权限校验。
- 登录日志、操作日志和审计日志
- 库存变化和设备任务必须关联操作人或系统服务身份

Task 6.3 当前实现提供 `IIdentityService`：PBKDF2 密码哈希、JWT Bearer 访问令牌、一次性刷新令牌、注销、改密和账号禁用；角色权限与仓库范围在高风险授权时同时校验。登录、刷新、注销、改密、账号禁用、角色/权限变更、设备任务和高风险授权写入 `IAuditLog`。API 默认只使用开发签名配置，生产必须显式配置至少 32 字符的密钥并将内存身份/审计实现替换为持久化实现。

Task 6.4 当前实现提供独立 `Warehouse.Wms.Web` 静态管理界面，覆盖工作台、入库、出库、库存/库位、设备任务、盘点和异常中心，并提供 PDA 扫码壳。页面只提交业务 API，不直接连接 PLC 或写寄存器；未知物理状态、二次授权和库存调整字段在页面中保持可见。界面自动化检查通过后仍必须由负责人按 [`docs/user-guide.md`](docs/user-guide.md) 完成人工核心流程确认。

### 6.12 管理界面

- 工作台
- 入库单、收货和上架
- 出库单、下架和复核
- 库存查询和库存流水
- 立体库库位可视化
- PLC、设备和装载点实时状态
- 作业任务中心
- 移库和盘点
- 异常中心
- 基础资料
- 用户权限
- 报表、日志和系统配置

#### 6.12.1 工作台、统计和实时点位交互设计

管理界面采用“作业优先 + 轻量实时监控”。工作台首屏以入库、出库、移库、盘点待办和快捷入口为主，同时保留 PLC/网关、堆垛机或设备、装载点、告警和数据新鲜度的只读状态卡片。操作员、主管和维护人员按角色看到不同的快捷入口和指标，但不改变业务状态机；监控卡片不得直接写 PLC 寄存器。

库存/库位页面增加点位视图，按仓库、库区、巷道、货架、层和库位逐级筛选。第一版采用可缩放的二维货架网格和层级选择器，点击库位打开详情抽屉，显示库位、托盘、物料、批次/有效期、数量、重量、库存状态、最近任务、资源锁、装载点和最近设备观察时间。点位颜色必须辅以文字或图标，业务状态至少区分空闲、占用、预留、锁定、设备执行中、异常/物理未知和禁用。物理未知、锁定、离线或过期快照不得被界面当作空闲，也不能通过点位视图直接改库存或设备状态。

统计页面提供库存总量/重量、库位利用率、入库量、出库量、移库量、盘点差异、设备任务成功率和异常数量。统计周期可配置为小时、日、周和月，默认日汇总；汇总批次使用周期、范围、来源版本和幂等键，重复执行安全重放，失败不覆盖最近成功结果。页面使用 KPI、趋势、分区利用率、任务状态分布和盘点差异列表，并支持跳转到单据、任务、托盘和异常详情。实时值与已完成周期汇总分开显示，所有结果显示生成时间和新鲜度。

点位和统计均为只读读模型。第一版默认使用短周期轮询；未来设备回调必须先进入统一观察/Inbox 处理服务。接口建议为 `/api/reports/summary`、`/api/reports/trends`、`/api/warehouse/points`、`/api/warehouse/points/{locationCode}` 和 `/api/warehouse/pallets/{palletCode}/position`。详细交互规格见 [`docs/superpowers/specs/2026-08-25-wms-statistics-point-view-design.md`](docs/superpowers/specs/2026-08-25-wms-statistics-point-view-design.md)。

### 6.13 报表

- 实时库存和库存流水
- 库位利用率
- 入库、出库和移库明细
- 托盘全流程追踪
- 批次和有效期
- 作业耗时和任务成功率
- PLC 在线率和报警记录
- 盘点差异
- 操作员工作量
- Excel 导入和导出

Task 7.1 增加只读报表 API：库存、库位利用率、入库/出库、移库、托盘轨迹、盘点差异和设备报警。后续统计任务增加按周期幂等汇总和可视化读模型；当前报表通过 `IReportsReadModel` 注入开发安全的内存快照，库位利用率按容量计算且零容量返回 0；接入 SQL 投影前不得将内存快照当作生产报表。

### 6.14 系统运维

- 数据库备份、恢复和迁移
- 健康检查和运行监控
- 配置管理和敏感配置隔离
- 日志查询、归档和清理
- 服务重启后的任务恢复
- 数据字典和编号规则
- 开发、测试、生产环境隔离
- 关键异常告警

Task 7.1 增加 API、数据库、Worker、设备网关和 Outbox/Inbox 健康聚合接口。健康结果区分 `Healthy`、`Degraded` 和 `Unhealthy`；设备离线、数据库探针失败或消息重放不会自动释放物理资源。`docs/operations.md` 和 `scripts/recovery-drill.ps1` 规定仅使用开发配置、模拟网关和临时目录执行恢复演练，覆盖迁移脚本生成、文件备份恢复、Worker 重启、PLC 离线/超时/物理未知和消息重放；真实数据库恢复、PLC 异常和账实核对仍需人工/现场门禁。

本地数据库验收通过 `ConnectionStrings__WmsDb` 向 EF Core 设计时工厂注入开发连接串；`scripts/verify.ps1` 可发现 PATH 或 Docker Desktop 标准目录中的 `docker.exe`，并在 SQL Server 容器内成功执行 readiness 查询后才运行迁移。该配置仅用于开发/测试数据库，不允许填入生产连接串。

Task 7.2 增加 [`docs/pilot-runbook.md`](docs/pilot-runbook.md)、[`docs/rollback-runbook.md`](docs/rollback-runbook.md) 和 [`tests/Warehouse.DeviceGateway.ContractTests/FieldRegressionChecklist.md`](tests/Warehouse.DeviceGateway.ContractTests/FieldRegressionChecklist.md)，冻结只读监控、单库区双跑、受控试点、人工对账、急停/断网/断电/PLC 重启/服务重启、未知结果和人工接管顺序。现场试点必须有现场、仓储、设备/PLC、WMS 运维和安全负责人签字；在真实设备回归和账实核对前只能标记 `BLOCKED`/`FIELD_PENDING`，不得修改旧 PLC 时序或把模拟测试当作 `FIELD_VERIFIED`。

### 6.15 可选外部集成

系统不依赖外部系统，但预留版本化 API 或消息接口，用于未来对接 ERP、MES 或其他系统。

- 外部系统只能通过集成适配器访问 WMS。
- 禁止外部系统直接读写 WMS 数据表。
- 外部请求必须有幂等键和来源标识。
- 外部接口不可用时，本地仓储作业不受影响。
- 外部回传失败必须进入待同步队列，不得重复执行 PLC 作业。

Task 8.1 固化 `/api/integrations/v1` 契约：入库通知、出库请求、取消请求、状态查询、结果回传和库存同步均必须携带来源、版本、幂等键和原始报文摘要。接收端只做契约校验并写入 Outbox，不直接调用 PLC 或修改库存；同一幂等键只能排队一次。`Wms:ExternalIntegrationsEnabled` 默认关闭，关闭时返回 `INTEGRATION_DISABLED` 且手工建单和本地仓储作业继续可用。Task 9.3 已将 SQL Server `IIntegrationOutbox` 接入 `Wms:PersistenceMode=SqlServer` 组合，并保留 `InMemory` 模式用于无数据库开发和契约测试；发布 Worker、外部系统真实联调和现场门禁仍待后续任务。

### 6.16 旧系统仓储规则基线

旧程序的入库、出库、移库、自动运行和盘点规则已形成第一阶段兼容基线，详细盘点和当前源码行号索引见 [`docs/legacy-warehouse-rules.md`](docs/legacy-warehouse-rules.md)。基线只描述现场已有行为，不替代新 WMS 的业务设计；行号只用于审计复核，不构成新系统接口契约。

必须保留的兼容输入包括：

- `ShelfStatus` 的现场含义：`0` 空、`1` 有货、`2` 停用、`-1` 不存在。
- 入库、出库、移库工作模式和触发寄存器映射，以及装载点 0/1 与重量 A/B 的对应关系。
- 入库目标库位为空且装载点有货；出库源库位有货且装载点为空；移库源有货、目标为空、设备空闲的现场前置约束。
- 出库前捕获托盘号、等待设备完成、必要时兼容“观察到运行后回到空闲”的完成判断。
- 盘点 PLC 范围 `A1`-`A8`、托盘 `prefix-number` 闭区间、按托盘/货架/储位排序、单 PLC 单活动任务和逐条明细上架。

不得直接继承的旧行为包括：ERP 存储过程取任务或回写单据、PLC 调用链修改业务库存、被注释的库位状态更新、移库源库位使用错误货架参数、按托盘移库目标托盘语义冲突，以及不同入口之间未统一的重量阈值。新 WMS 必须通过设备兼容层调用旧 PLC 接口，并在设备完成确认后由自己的事务更新任务、托盘、库位、库存和流水。

所有重量阈值、完成码、库位/装载点更新时间点和盘点期间并发限制均标记为现场确认项，未确认前不得固化为不可配置规则。

## 7. 核心业务约束

- 一个托盘在同一时刻只能位于一个库位、装载点或执行中的任务中。
- 一个库位在同一时刻最多容纳符合配置数量的托盘或容器。
- 库存不得无来源增加、减少或改变位置。
- 已发送 PLC 的任务不能直接删除，只能取消、失败、完成或进入人工处置。
- 任务状态必须区分未下发取消、停止请求、停止确认、停止失败、物理状态未知和人工处置；设备已经进入 `SentToPlc` 或 `Executing` 后，取消不等于物理动作已停止。
- PLC 执行期间不得持有跨设备等待的数据库长事务。设备命令和结果使用幂等键、乐观并发版本、资源锁、Outbox/Inbox、状态历史和重启后的状态查询/对账；每次业务状态和库存变更使用独立短事务提交。
- PLC 返回成功后，业务状态和库存更新必须具备幂等和事务一致性；设备未知结果不得直接扣减或释放库存。
- 重复请求、网络重试和服务重启不得造成重复库存变化。
- 所有库存调整必须记录原因和审计信息。
- “人工完成”必须实现为“人工确认物理结果并结案”，要求二次权限、托盘实际位置、源/目标库位、设备状态、处理原因和库存校正流水，不能只把任务状态改为成功。

取消、停止和人工结案协议：

- `Created`、`Allocated`、`Queued` 仅表示设备尚未下发；取消后进入 `Canceled`，并由资源协调器释放调度占用。
- `Dispatching` 在设备调用尚未开始时取消必须回到 `Queued` 并释放调度占用；调用已开始但没有确定结果时必须进入 `TimedOut` -> `PhysicalStateUnknown`，不得当作未发送。
- `SentToPlc`、`Executing` 的取消必须先进入 `CancelRequested`、`StopRequested` 并调用 `RequestStopAsync`。只有设备明确返回 `StopConfirmed` 才能进入 `Canceled` 并释放资源；`StopFailed` 和 `PhysicalStateUnknown` 保持资源占用并等待处置。
- “人工确认物理结果并结案”必须通过 `ICurrentUser` 识别当前操作者和 `IRiskAuthorizationService` 二次授权，记录原因、设备状态、托盘实际位置、源库位、目标库位以及完整库存校正流水。操作结果进入 `ManualIntervention`，不能改写为 `Succeeded`；重复确认和缺少任一必填字段必须拒绝。

## 8. 非功能要求

- 可靠性：关键服务重启后可恢复任务状态。
- 一致性：单据、任务、库存、托盘和库位状态可核对。
- 可追溯：任意托盘和库存变化均可追踪完整历史。
- 安全性：最小权限、敏感配置隔离、关键操作审计。
- 可测试性：业务规则与 PLC 通信解耦，支持模拟 PLC。
- 可维护性：模块职责清晰，不在控制器或 PLC 服务中直接编写库存业务。
- 可部署性：本地网络环境可独立部署，不依赖公网或 ERP。
- 可验证性：本地验收至少包括 `dotnet restore`、`dotnet build --no-restore`、`dotnet test --no-build`、数据库迁移和健康检查；模拟 PLC 未配置时仍可启动。

## 9. 建设阶段建议

### 第一阶段：基础闭环

- 基础资料
- PLC/WCS 通信
- 库位和托盘
- 入库、出库和移库
- 实时库存和库存流水
- 任务调度及异常恢复
- PLC 模拟器、设备契约测试和本地自动化验收
- 用户权限和基础管理页面
- SQL Server 业务数据持久化（库存余额、库存流水和幂等键先行，随后扩展单据、任务、锁和审计）

### 第二阶段：仓储完善

- 盘点
- 批次和有效期
- 库位策略
- 复核和质量状态
- 报表和审计
- 现场回归、账实对账和恢复演练

### 第三阶段：优化和集成

- 调度优化
- 可视化监控
- 数据分析和告警
- 可选 ERP/MES/API 集成

### Agent 执行和现场门禁

- 实施计划是总路线图，Agent 每次只执行一个 Task，完成后提交修改文件、迁移、测试、命令结果、风险和人工确认项，等待批准后再进入下一项。
- `AGENT_VERIFIED` 只表示自动化构建、测试、迁移和静态检查有证据；`HUMAN_CONFIRMED` 表示负责人确认规则和操作；`FIELD_VERIFIED` 表示真实设备回归、异常恢复和账实核对完成；现场事实未确认时必须为 `BLOCKED`。完整状态词典见 `docs/status-dictionary.md`。
- 阶段顺序固定为：现场/范围冻结、系统骨架、设备契约和模拟器、基础资料与库存、任务调度与恢复、入库/出库/移库、盘点与权限、运维和现场试运行、可选集成。业务闭环不得先于统一任务调度器实现。
- 现场试运行必须有只读监控、试点、双跑、人工对账、切换条件和回滚手册；没有负责人签字不得切换生产作业或修改旧 PLC 时序。

## 10. 需求变更规则

每次需求新增、修改或删除，必须同步完成：

1. 修改本文档对应章节。
2. 更新文档版本和更新日期。
3. 在下方变更记录中登记变更内容和影响模块。
4. 若涉及数据、接口或状态流转，同步更新相关模型、接口契约和流程说明。
5. 实现代码、数据库迁移、测试和本文档必须保持一致。

## 11. 需求变更记录

| 日期 | 版本 | 变更内容 | 影响模块 |
| --- | --- | --- | --- |
| 2026-08-24 | 0.1 | 确定系统脱离 ERP 独立运行，覆盖仓储全流程；采用模块化单体 WMS 和独立 PLC/WCS 服务 | 全系统 |
| 2026-08-24 | 0.2 | 确认旧程序正在现场使用；保留现有 PLC 接口、寄存器协议和设备时序，新 WMS 通过兼容适配器调用，不直接改动生产 PLC 行为 | PLC/WCS、任务调度、全系统边界 |
| 2026-08-25 | 0.3 | 将旧程序的 PLC、寄存器、库位、托盘、装载点和设备时序记录确定为第一阶段现场基线；新 WMS 暂时调用原有 PLC 接口，明确过渡调用边界和退出条件 | 现场基线、设备网关、任务调度、库存和部署 |
| 2026-08-25 | 0.4 | 盘点旧程序入库、出库、移库、自动运行和盘点范围规则，记录寄存器时序、装载点阈值差异、旧接口缺陷及新系统禁止继承项 | 入库、出库、移库、盘点、设备网关、库存和任务调度 |
| 2026-08-25 | 0.5 | 根据实施计划评审意见细化 Agent 单 Task 执行协议、阶段依赖、短事务与 Outbox/Inbox 一致性、取消/停止/物理未知状态、人工物理结果结案和 AGENT/HUMAN/FIELD 三类门禁 | 任务调度、设备网关、库存事务、异常处置、现场试运行和开发流程 |
| 2026-08-25 | 0.6 | 明确第一版网关为 WMS Worker 内类库、旧 PLC 幂等能力分级和超时未知策略；新增异常工作项中心、Excel 导入任务、轮询/回调统一结果处理及提前的高风险授权抽象；将 PLC 模拟器和自动化测试纳入第一阶段 | 设备网关、任务调度、异常中心、入库、出库、权限、测试和实施阶段 |
| 2026-08-25 | 0.7 | 执行 Task 0.1：为旧系统入库、出库、移库、自动任务、盘点和寄存器基线补充当前源码行号索引，并明确行号仅用于审计复核 | 现场基线、入库、出库、移库、盘点、设备网关和设计文档 |
| 2026-08-25 | 0.8 | 执行 Task 0.2：建立 PLC 连接、寄存器信号、仓储编码、计量、并发和超时基线表；所有未由源码或现场证据确认的参数标记为 `BLOCKED` | 现场基线、设备网关、任务调度、基础资料和现场门禁 |
| 2026-08-25 | 0.9 | 执行 Task 0.3：冻结第一版范围、入口优先级和入库/出库/移库/盘点/库存/设备任务状态词典；增加 `Dispatching` 发送尝试状态并禁止用 `Succeeded` 替代取消、物理未知或人工处置 | 业务范围、单据、库存、任务调度、设备网关、异常和权限 |
| 2026-08-25 | 1.0 | 执行 Task 1.1：建立 .NET 8 WMS 解决方案、Domain/Application/Infrastructure/API/DeviceGateway 项目边界、单元/集成/设备契约测试项目和无生产凭据的开发配置模板；验证无外部依赖时 API 健康检查可用 | 系统骨架、项目依赖、API 运维、自动化测试 |
| 2026-08-25 | 1.1 | 执行 Task 1.2：建立本地开发质量门禁、SQL Server Compose 模板、统一验证脚本和脚本顺序回归测试；无迁移的骨架阶段将数据库检查标记为不适用，存在迁移但缺少 Docker/EF 时保持阻塞退出 | 本地开发、数据库迁移、自动化测试、运维验收 |
| 2026-08-25 | 1.2 | 执行 Task 2.1：冻结设备任务请求、设备结果、能力标志、轮询/回调观察和 `RequestStopAsync` 契约；明确幂等键、任务号查询能力不足时不得宣称恰好一次执行 | 设备网关、任务调度、状态词典、契约测试 |
| 2026-08-25 | 1.3 | 执行 Task 2.2：加入可配置延迟、成功/失败/离线/超时/未知、报警、停止结果、能力分级、幂等去重和共享状态重启恢复的模拟设备网关；无去重/查询/停止能力时不伪造设备能力 | 模拟设备、设备契约、任务恢复、自动化测试 |
| 2026-08-25 | 1.4 | 执行 Task 2.3：增加旧 PLC HTTP API 只读适配器和能力探测；旧 API 触发成功仅映射为 `Accepted`，任务号去重/查询、停止和回调能力保持 `BLOCKED`，发送超时或非法响应进入未知/离线处置 | 设备网关、任务调度、现场基线、异常处置和自动化测试 |
| 2026-08-25 | 1.5 | 执行 Task 3.1：建立独立 WMS 基础资料实体、SQL Server DbContext、唯一业务索引、库位容量/现场映射、托盘版本与归属约束、首个迁移和开发样例种子；不读取 ERP 或现场数据库 | 基础资料、托盘、库位、数据库迁移和自动化测试 |
| 2026-08-25 | 1.6 | 执行 Task 3.2：建立库存状态、库存余额和不可篡改库存流水服务；增加幂等、并发锁定、负库存防护、事务回滚、来源审计和流水重算测试 | 库存、托盘、库位、业务单据、审计和自动化测试 |
| 2026-08-25 | 1.7 | 执行 Task 4.1：建立仓储任务 16 状态枚举、合法状态迁移、操作者/原因/错误码/UTC 时间状态历史、终态保护，以及取消、停止、物理未知和人工处置语义 | 任务状态机、设备调度、异常处置、库存锁和审计 |
| 2026-08-25 | 1.8 | 执行 Task 4.2：建立任务幂等键、租约资源锁、Outbox/Inbox 消息实体及短事务边界；增加并发获取、版本冲突、消息抢占/发布和重复结果去重测试 | 任务调度、设备网关、库存锁、数据库持久化、恢复和审计 |
| 2026-08-25 | 1.9 | 执行 Task 4.3：实现按设备串行和优先级调度、有限重试、统一轮询/回调结果处理、未知结果保护及 Worker 重启恢复测试 | 任务调度、设备网关、Outbox/Inbox、异常处置、库存锁和恢复 |
| 2026-08-25 | 2.0 | 补充 Task 4.3 的发送取消边界、设备任务号归属校验和结果版本单调去重，避免未知设备动作被误标失败或旧观察覆盖新状态 | 任务状态机、设备调度、结果处理、异常处置和审计 |
| 2026-08-25 | 2.1 | 执行 Task 5.2：增加入库库位自动推荐/人工指定、尺寸重量容量和运行状态筛选、装载点有货校验、待入库库存/托盘/库位/装载点资源锁及统一调度器提交；设备确认前不改变待入库库存，重复幂等请求不重复下发 | 入库、库位分配、资源锁、设备任务和异常处置 |
| 2026-08-25 | 2.2 | 执行 Task 5.3：增加入库设备结果对账和幂等库存落账；成功后更新目标库位库存流水并释放资源，失败/超时/物理未知保持待入库和锁定，全部任务成功后才完成入库单 | 入库、库存、任务调度、资源锁、异常处置和审计 |
| 2026-08-25 | 2.3 | 执行 Task 5.4：定义出库状态、指定批次/托盘/库位和确定性库存分配，支持部分数量并创建库存、托盘、库位幂等资源锁；设备执行和最终扣减留待 Task 5.5 | 出库、库存锁、任务调度和异常处置 |
| 2026-08-25 | 2.4 | 执行 Task 5.5：增加出库设备任务和装载点锁定、设备结果统一处理、托盘号/重量/物理状态复核、复核幂等库存扣减；设备失败、超时、未知结果和复核差异保持库存与资源锁不变 | 出库、设备网关、装载点、库存、资源锁、异常处置和审计 |
| 2026-08-25 | 2.5 | 执行 Task 5.6：增加移库状态、源/目标/托盘校验、资源锁、统一 Transfer 设备任务和成功后的幂等库存移动；目标占用、失败、未知结果和重复移库均受保护，跨 PLC/跨巷道及现场托盘位置仍待确认 | 移库、库存、托盘、库位、设备网关、资源锁和异常处置 |
| 2026-08-25 | 2.6 | 执行 Task 6.1：增加盘点任务和明细状态、全库/库区/物料/批次/托盘/位置范围筛选、A1-A8 与 prefix-number 闭区间兼容、确定性排序、账面/实盘/差异记录和共享调度器设备盘点队列；差异调整留待 Task 6.2 | 盘点、库存、设备调度、装载点和异常处置 |
| 2026-08-25 | 2.1 | 执行 Task 4.4：实现未下发取消资源释放、停止请求结果处理、物理未知保护、人工确认物理结果并结案、二次高风险授权和库存校正流水审计 | 任务状态机、设备网关、资源锁、异常处置、权限和审计 |
| 2026-08-25 | 2.2 | 补充 Task 4.4 停止响应幂等键校验，串线或不匹配的停止结果进入物理未知并禁止释放资源 | 设备停止协议、任务状态、资源锁和异常处置 |
| 2026-08-25 | 2.3 | 执行 Task 4.5：建立异常工作项领域实体、异常来源/类型/严重程度/物理状态和资源快照，增加 source+externalKey+taskId 幂等合并、关闭后重开、重试/停止/重新分配/人工确认/库存校正动作契约、二次授权、竞态保护和异常审计；明确当前为内存仓储和适配器，生产持久化、真实权限和现场对账仍未完成 | 异常中心、任务状态机、设备网关、库存锁、权限、审计和 API |
| 2026-08-25 | 2.4 | 补充 Task 4.5 未知物理状态关闭保护：未完成设备对账或人工物理确认时，异常工作项不得直接关闭 | 异常处置、物理状态、资源锁和审计 |
| 2026-08-25 | 2.5 | 执行 Task 5.1：建立入库单和明细领域模型、状态迁移、手工建单、批次/有效期、托盘绑定、重量采集、收货幂等及 `PendingInbound` 待入库记录；明确当前为内存应用契约，收货不直接写库位实存 | 入库、库存状态、托盘绑定、任务上架和自动化测试 |
| 2026-08-25 | 3.2 | 执行 Task 6.2：增加盘点差异工作项、明盘/盲盘视图、复盘、差异原因、冻结策略、二次授权和审计；授权后通过 `InventoryService.AdjustAsync` 以调整编号幂等写入库存流水，失败保持可重试；增加移动托盘上架预约的 `Reserved`/`Sent`/`Completed` 状态区分 | 盘点、库存调整、库存流水、权限、审计、设备任务和异常处置 |
| 2026-08-25 | 3.3 | 执行 Task 6.3：接入开发环境 JWT Bearer、PBKDF2 密码哈希、刷新令牌轮换、注销、密码修改、账号禁用、用户/角色/功能权限、仓库范围授权、高风险二次授权和内存审计；增加用户/角色 API 与未授权请求统一 401/403/409 响应，生产持久化和真实密钥留待后续门禁 | 认证、用户、角色、权限、仓库范围、高风险操作、审计和 API |
| 2026-08-25 | 3.4 | 执行 Task 6.4：新增独立 `Warehouse.Wms.Web` 管理界面和 PDA 扫码壳，覆盖工作台、入库、出库、库存/库位、设备任务、盘点和异常中心；前端只调用业务 API，不执行 PLC 时序，并补充操作员/主管流程说明 | 管理界面、PDA、入库、出库、库存、任务、盘点、异常、权限和操作文档 |
| 2026-08-25 | 3.5 | 执行 Task 7.1：增加库存/库位利用率/入出库/移库/托盘追踪/盘点差异/设备报警报表 API、五类组件健康聚合、开发安全的恢复演练脚本和运维手册；明确当前内存报表读模型、模拟设备和 HUMAN/FIELD 门禁 | 报表、API、数据库、Worker、设备网关、Outbox/Inbox、恢复和运维 |
| 2026-08-25 | 3.6 | 执行 Task 7.2：新增现场试运行手册、回滚手册和设备现场回归清单，明确只读监控、单库区双跑、人工对账、急停/断网/断电/重启/未知结果/人工接管、成功/停止/回滚条件、责任人和证据留存；真实现场签字保持 `BLOCKED`/`FIELD_PENDING` | 现场试点、回滚、设备回归、运维、库存对账和门禁 |
| 2026-08-25 | 3.7 | 执行 Task 8.1：定义 v1 外部入库/出库/取消/状态/结果/库存同步接口，统一来源、版本、幂等键、关联号和 SHA-256 报文摘要；接收端写入可替换 Outbox，集成默认关闭且不影响手工 WMS | 外部集成、Outbox、幂等、API 契约和独立运行 |
| 2026-08-25 | 3.8 | 修复本地质量门禁：支持 Docker Desktop 标准安装路径、等待 SQL Server 实际可查询，并让 EF 设计时工厂优先使用 `ConnectionStrings__WmsDb`；开发迁移和健康检查完整通过 | 开发环境、数据库迁移、Docker、质量门禁和运维文档 |
| 2026-08-25 | 3.9 | 执行 Task 5.1A：增加版本化 Excel 入库/出库模板、整批校验、结构化错误报告、来源键+文件摘要幂等和导入 API；成功导入只创建 WMS 单据，不下发 PLC | 入库、出库、导入 API、错误处置、测试和操作文档 |
| 2026-08-25 | 3.10 | 执行 Task 8.2：补齐 API 运行时组合根，默认注册模拟设备网关、统一任务调度器及所有控制器应用服务；新增真实启动和逐控制器依赖解析集成测试，确保无 ERP/真实 PLC 时 API 可启动 | API 启动、依赖注入、模拟设备、任务调度、异常作用域和集成测试 |
| 2026-08-25 | 3.11 | 新增 Task 9.1：将库存余额、不可篡改库存流水和操作幂等键接入 SQL Server 短事务持久化；保留仅用于无数据库开发/契约测试的内存模式，增加重启恢复、唯一幂等和乐观并发验收 | 库存、数据库迁移、事务边界、幂等、恢复、API 配置和测试 |
| 2026-08-25 | 3.12 | 执行 Task 9.2A：将 Outbox/Inbox 消息实体接入 SQL Server，增加唯一业务索引、乐观版本、租约抢占、发布/处理/失败短事务和重复结果去重；暂不接入调度器或现场 Worker | Outbox/Inbox、任务调度、数据库迁移、消息恢复、幂等和测试 |
| 2026-08-25 | 3.13 | 执行 Task 9.3：将外部集成 Outbox 从内存实现切换为复用 SQL Server 消息仓储的持久化适配器；SQL 模式注册消息 store 和 SQL outbox，内存模式及集成关闭行为保持不变；发布 Worker、真实外部联调和 HUMAN/FIELD 门禁后置 | 外部集成、Outbox、数据库组合、幂等、API 配置和测试 |
| 2026-08-25 | 3.14 | 确认管理界面采用“作业优先 + 轻量实时监控”；新增可配置周期统计、可视化图表、按巷道/货架/层/库位查看实时物品的点位视图、过期/物理未知提示和只读查询契约；先完成设计和计划拆分，后续 Task 统一实现 | 管理界面、报表读模型、统计任务、库存/库位查询、设备观察、权限和 API |
| 2026-08-25 | 3.15 | 执行 Task 9.4：设备命令在调用网关前通过可选 Outbox 短事务入队并抢占，确定性结果发布成功，失败/物理未知进入不可自动重发状态；SQL 模式注册任务命令 Outbox，内存测试保持兼容 | 任务调度、设备网关、Outbox、重试、物理未知、SQL 组合和恢复 |
| 2026-08-26 | 3.16 | 完成 Task 6.5 界面设计冻结：确认作业优先工作台保留轻量实时监控，新增周期统计与可视化图表、二维实时点位视图、过期/离线/物理未知提示和只读查询边界；Task 6.6 后续实现暂未开始 | 管理界面、统计读模型、点位查询、设备观察、权限、API 和后续实现计划 |
| 2026-08-26 | 3.17 | 执行 Task 6.6：增加小时/日/周/月统计批次幂等和可配置调度边界、统计摘要/趋势 API、按空间层级筛选的点位只读 API、托盘定位、过期/物理未知提示和 Web 统计/点位视图；统计按仓库范围查询，前端动态数据渲染转义 | 统计读模型、周期汇总、报表 API、库位/托盘查询、管理界面、设备观察和测试 |
| 2026-08-26 | 3.18 | 新增 Task 9.5：任务实体、状态历史、资源锁和任务幂等键使用 SQL Server 业务真相；增加任务版本与锁版本乐观并发、资源业务键唯一、租约抢占/续租/释放、短事务边界和重启恢复要求；内存实现限定为显式测试/开发模式 | 任务持久化、状态机、资源锁、幂等、恢复、EF Core 迁移、运维 |
| 2026-08-26 | 3.19 | 明确 Task 9.5 仅完成任务持久化基础设施；新增 Task 9.6 将调度器/业务闭环接入 SQL 持久化并实现 Worker 重启恢复，完成前不得将进程内调度缓存表述为生产业务真相 | 任务调度、运行时组合、重启恢复、SQL 业务真相、验收门禁 |
| 2026-08-26 | 3.20 | 执行 Task 9.6 初版：持久化设备调度上下文并支持队列/执行中任务重启恢复；Worker 对账设备任务号，未知结果进入物理状态未知；入库、出库、移库和盘点资源锁增加持久化接入，保留显式内存开发回退 | 任务调度、Worker、设备网关、资源锁、入库、出库、移库、盘点、数据库迁移和恢复 |
| 2026-08-26 | 3.21 | sol 复审发现 Task 9.6 仍有验收阻塞：重启后设备串行占用未恢复，出库装载点锁和入库锁释放仍使用内存对象，业务闭环上下文未完整重建；修复完成前保持进行中状态 | 任务调度、重启恢复、出库、入库、资源锁、业务闭环和验收门禁 |
| 2026-08-26 | 3.22 | 修复 Task 9.6 的设备串行占用和入库/出库持久化锁释放；新增工作流引用、调度快照和 `BlockedMissingBusinessState` 状态，恢复时无法重建业务聚合则显式阻塞；自动化测试继续通过，完整 SQL 跨进程验收仍待执行 | Worker、任务调度、入库、出库、资源锁、业务恢复、数据库迁移和异常处置 |
| 2026-08-26 | 3.23 | 完成 Task 9.6 的 SQL 设备租约、Worker 重启对账、有效/无效工作流快照恢复、盘点锁释放和 Docker SQL 定向验收；明确业务聚合尚未跨进程重建，新增 Task 9.7 作为后续持久化与恢复任务，Task 9.6 状态保持 `DONE_WITH_CONCERNS` | Worker、任务调度、设备租约、资源锁、业务聚合恢复、数据库迁移和验收门禁 |
| 2026-08-26 | 3.24 | 修正 Task 9.7 前置条件：Task 9.6 的调度/设备恢复子集通过且无 P0 即可进入业务聚合持久化；Task 9.6 已知业务聚合缺口不再阻塞其后续专门任务 | 实施计划依赖、业务聚合持久化、Worker 恢复和验收门禁 |
| 2026-08-26 | 3.25 | 将业务聚合恢复拆分为 Task 9.7A（通用持久化基础设施及入库/出库）和 Task 9.7B（移库/盘点），每项独立迁移、重启恢复、幂等和并发验收 | 入库、出库、移库、盘点、数据库迁移、Worker 恢复和验收门禁 |
| 2026-08-26 | 3.26 | 完成 Task 9.7A：业务快照 SQL/InMemory 存储、入库收货与 PendingInbound 稳定恢复、上架任务/库位分配恢复、出库分配/复核恢复、缺失业务快照阻断和设备任务号未知处置；Task 9.7B 仍未开始 | 入库、上架、出库、复核、任务恢复、幂等、资源锁和 Worker 启动恢复 |
| 2026-08-26 | 3.27 | 执行 Task 9.7B：移库、盘点任务/明细、盘点差异和上架预约接入版本化业务快照；增加稳定身份、状态恢复、幂等回放、资源锁上下文和 Worker 启动恢复；完整 SQL 专用实体/现场恢复仍待后续门禁 | 移库、盘点、库存结算、差异调整、资源锁、任务恢复和数据库 |
| 2026-08-26 | 3.28 | 补充 Task 9.7B 的 scoped 盘点差异恢复、移库 SQL 重启测试和验证证据；保留通用业务快照表及跨进程并发 deadlock 风险 | Worker 恢复、移库、盘点、差异调整、SQL 验证和验收门禁 |
| 2026-08-26 | 3.29 | 为业务快照 SQL 保存增加 deadlock/唯一键竞争有限重试，并加入跨进程同版本写入验收；版本冲突仍由业务层明确返回 | SQL 业务快照、并发恢复、移库、盘点和验收门禁 |
| 2026-08-26 | 3.30 | 执行 Task 9.8A：硬化业务快照 SQL 保存/幂等注册的有限重试与明确版本/摘要冲突语义；同版本并发仅允许一个提交；API composition SQL 测试改为环境变量夹具，无 SQL 环境稳定跳过 | SQL 业务快照、幂等、并发恢复、API 组合和测试门禁 |
